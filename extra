 # customer_number_pattern = r"Customer Number:\s*(\d+)"
    # invoice_number_pattern = r"Invoice Number:\s*(\d+)"
    # ship_number_pattern = r"Ship Number:\s*(\d+)"


# customer_number = re.search(customer_number_pattern, text)
    # invoice_number = re.search(invoice_number_pattern, text)
    # ship_number = re.search(ship_number_pattern, text)


 # customer_number = customer_number.group(1)
        # invoice_number = invoice_number.group(1)
        # ship_number = ship_number.group(1)


 # if customer_number:
        #     st.write( customer_number)
        # if invoice_number:
        #     st.write(invoice_number)
        # if ship_number:
        #     st.write(ship_number)


 # for page_text in pdf_text:
        #     st.write(page_text)




# poNo, invoiceDate,invoiceNo = extractText("temp.pdf")
            # if poNo:
            #     st.write(poNo)
            # if invoiceDate:
            #     st.write(invoiceDate)
            # if invoiceNo:
            #     st.write(invoiceNo)



        # st.write(extractPdf("temp.pdf"))
        # pdfReader=display_pdf_content("temp.pdf")
        # for page_text in pdfReader:
        #     st.write(page_text)